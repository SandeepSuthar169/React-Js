console.log(`hello world`);

const userAdd = (user: string, password: string): string => {
    return `Hello my name is ${user} and password is ${password}`
} 

console.log(userAdd("sandeep", "suthar"));
